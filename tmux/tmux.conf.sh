# Local note
#
# The imported tmux reference was removed from this repository because it
# contained private, machine-specific paths and keybindings.
#
# Use ~/mydotfiles/tmux/tmux.conf as the active tmux configuration.
