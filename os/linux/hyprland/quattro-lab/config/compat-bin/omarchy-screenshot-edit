#!/usr/bin/env bash
set -Eeuo pipefail

[[ $# -eq 1 && -f $1 ]] || {
  printf 'Usage: omarchy-screenshot-edit <image>\n' >&2
  exit 2
}
command -v satty >/dev/null 2>&1 || {
  printf 'ERROR: Satty is not installed.\n' >&2
  exit 127
}

exec satty --filename "$1" --output-filename "$1"
