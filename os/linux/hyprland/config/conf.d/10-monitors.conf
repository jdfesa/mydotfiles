# Portable default: use the preferred mode and discover output position.
monitor = , preferred, auto, 1

# Current arch-desktop output, kept as documentation until runtime validation:
# monitor = HDMI-A-2, 1920x1080@60, 0x0, 1
