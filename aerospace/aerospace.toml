# AeroSpace Configuration for User
# Based on system context: macOS (Hackintosh), Sketchybar, HYPER Key (Splits).
# HYPER = cmd-alt-ctrl-shift

# Config version
config-version = 2

# Start AeroSpace at login
start-at-login = true

# Normalizations
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# Layout
accordion-padding = 30
default-root-container-layout = 'tiles'
default-root-container-orientation = 'auto'

# Mouse follows focus
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# GAPS Configuration
[gaps]
    inner.horizontal = 8
    inner.vertical =   8
    outer.left =       8
    outer.bottom =     8
    outer.top =        45  # Reserved for Sketchybar
    outer.right =      8

# Monitor and Workspace Assignments (1-8)
[workspace-to-monitor-force-assignment]
    1 = 'main'       # Terminal / Dev
    2 = 'main'       # Web Browsing
    5 = 'main'       # Media / Music
    7 = 'main'       # Extras
    3 = 'secondary'  # Productivity / Notes
    4 = 'secondary'  # Reading / Docs
    6 = 'secondary'  # Communications (Chat)
    8 = 'secondary'  # System / Files

# App Routing Rules (Baseline - Manual persistence handles the rest)
[[on-window-detected]]
if.app-name-regex-substring = 'Ghostty'
run = 'move-node-to-workspace 1'

[[on-window-detected]]
if.app-name-regex-substring = 'TablePlus'
run = 'move-node-to-workspace 1'

[[on-window-detected]]
if.app-name-regex-substring = 'Obsidian'
run = 'move-node-to-workspace 3'

[[on-window-detected]]
if.app-name-regex-substring = 'Spotify'
run = 'move-node-to-workspace 5'

[[on-window-detected]]
if.app-name-regex-substring = 'Telegram'
run = 'move-node-to-workspace 6'

[[on-window-detected]]
if.app-name-regex-substring = 'Discord'
run = 'move-node-to-workspace 6'

# Key Mapping Preset
[key-mapping]
    preset = 'qwerty'

# =================================================================
# MAIN BINDING MODE
# =================================================================
[mode.main.binding]

    # Global Actions
    cmd-alt-ctrl-shift-t = 'exec-and-forget open -n -a /Applications/Ghostty.app'
    
    # Modes Entry Points
    cmd-alt-ctrl-shift-semicolon = 'mode service'
    cmd-alt-ctrl-shift-r = 'mode resize'
    cmd-alt-ctrl-shift-slash = 'mode layout'
    cmd-alt-ctrl-shift-p = 'mode persistence'

    # -----------------------------------------------------------------
    # NAVIGATION / SEEING -> HYPER (cmd-alt-ctrl-shift)
    # -----------------------------------------------------------------
    cmd-alt-ctrl-shift-h = 'focus left'
    cmd-alt-ctrl-shift-j = 'focus down'
    cmd-alt-ctrl-shift-k = 'focus up'
    cmd-alt-ctrl-shift-l = 'focus right'

    # Workspace Switching
    cmd-alt-ctrl-shift-1 = 'workspace 1'
    cmd-alt-ctrl-shift-2 = 'workspace 2'
    cmd-alt-ctrl-shift-3 = 'workspace 3'
    cmd-alt-ctrl-shift-4 = 'workspace 4'
    cmd-alt-ctrl-shift-5 = 'workspace 5'
    cmd-alt-ctrl-shift-6 = 'workspace 6'
    cmd-alt-ctrl-shift-7 = 'workspace 7'
    cmd-alt-ctrl-shift-8 = 'workspace 8'

    cmd-alt-ctrl-shift-tab = 'workspace-back-and-forth'

    # -----------------------------------------------------------------
    # MOVING / ARRANGING -> ALT
    # -----------------------------------------------------------------
    alt-h = 'move left'
    alt-j = 'move down'
    alt-k = 'move up'
    alt-l = 'move right'

    # Move node to workspace
    alt-1 = 'move-node-to-workspace 1'
    alt-2 = 'move-node-to-workspace 2'
    alt-3 = 'move-node-to-workspace 3'
    alt-4 = 'move-node-to-workspace 4'
    alt-5 = 'move-node-to-workspace 5'
    alt-6 = 'move-node-to-workspace 6'
    alt-7 = 'move-node-to-workspace 7'
    alt-8 = 'move-node-to-workspace 8'

# =================================================================
# MODE: PERSISTENCE (Hyper + P)
# =================================================================
[mode.persistence.binding]
    # S = Snapshot (Save current state)
    s = ['exec-and-forget python3 ~/.mydotfiles/aerospace/scripts/save_layout.py', 'mode main']
    
    # L = Load (Restore from snapshot)
    l = ['exec-and-forget python3 ~/.mydotfiles/aerospace/scripts/restore_layout.py', 'mode main']
    
    esc = 'mode main'
    enter = 'mode main'

# =================================================================
# MODE: LAYOUT (Hyper + /)
# =================================================================
[mode.layout.binding]
    a = ['layout accordion horizontal vertical', 'mode main'] # Accordion Toggle
    t = ['layout tiles horizontal vertical', 'mode main']     # Tiles Toggle
    
    # Orientation (Tiles)
    v = 'layout vertical'
    h = 'layout horizontal'

    esc = 'mode main'
    enter = 'mode main'

# =================================================================
# MODE: RESIZE (Hyper + R)
# =================================================================
[mode.resize.binding]
    h = 'resize width -50'
    l = 'resize width +50'
    j = 'resize height +50'
    k = 'resize height -50'
    esc = 'mode main'
    enter = 'mode main'

# =================================================================
# MODE: SERVICE (Hyper + ;)
# =================================================================
[mode.service.binding]
    esc = ['reload-config', 'mode main']
    r = ['flatten-workspace-tree', 'mode main'] # Reset layout structure
    backspace = ['close-all-windows-but-current', 'mode main']

